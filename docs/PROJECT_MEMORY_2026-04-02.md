# Project Memory - 2026-04-02

## 1. Why This File Exists

This file is the durable recovery snapshot for the workspace. When the chat is interrupted, this should be the first operational handoff file to read.

## 2. Current Operating State

Snapshot date: `2026-04-02`

- Workspace: `D:\openlianghua`
- Git status: this directory is **not** a Git repository
- Active data source: `myquant`
- Dashboard URL: `http://localhost:8501`
- Dashboard control scripts:
  - `scripts/start_streamlit.ps1`
  - `scripts/streamlit_status.ps1`
  - `scripts/stop_streamlit.ps1`

Recent verified state:

- The dashboard is reachable locally after the latest refresh.
- `streamlit_status.ps1` remains the source of truth for real UI health.
- MyQuant terminal connectivity was restored on `2026-04-02` by launching the local `掘金量化终端`.

## 3. Latest Pipeline Refresh

The full MyQuant research pipeline was rerun to the exact cutoff date `2026-04-01`:

```powershell
python -m src.data.myquant_downloader --start-date 2018-01-01 --end-date 2026-04-01 --write-canonical
python -m src.data.myquant_enrich
python -m src.features.build_feature_panel
python -m src.models.train_linear
python -m src.models.train_lgbm
python -m src.models.train_ensemble
python -m src.agents.overlay_report
python -m src.agents.watch_plan
python -m src.agents.action_memo
```

After the historical refresh, the latest unlabeled inference path was also run on the same workspace state:

```powershell
python -m src.agents.overlay_inference_report
```

## 4. Data And Signal Snapshot

- `data/staging/myquant_data_quality.json` now says the cleaned MyQuant panel reaches `2026-04-01`.
- `000078.SZ` now has a valid `2026-04-01` daily bar in `data/staging/myquant_daily_bar.parquet`:
  - open `3.17`
  - high `3.45`
  - low `3.08`
  - close `3.45`
- The latest saved ensemble signal date is `2026-03-18`.
- That lag is still expected because the active label remains `ret_t1_t10`.
- The new latest unlabeled inference packet now bridges that gap:
  - latest feature date: `2026-04-01`
  - latest labeled history date: `2026-03-18`
  - inference universe size: `498`
  - selected feature count: `8`
  - skipped future-dependent filters: `can_enter_next_day`, `label_valid(ret_t1_t10)`

## 5. Watchlist / Position State

The watchlist currently contains one holding in `config/watchlist.yaml`:

- `000078.SZ`
- Name: `海王生物`
- Cost: `3.851`
- Shares: `15000`

Important change:

- The temporary manual mark price was removed after the `2026-04-01` real daily bar landed.
- The dashboard now values the holding from the latest local daily close directly.
- The `AI研判` page now supports one-click external OpenAI execution when `.env` contains a valid `OPENAI_API_KEY`.
- The holding page and generated `action_memo` now backwrite multi-round discussion state from both the historical verified overlay and the latest inference overlay.
- The watchlist summary table now also shows a one-line `盘前执行建议`, generated from multi-round discussion state plus key price levels.
- The `观察持仓` page now shows whether a holding price is using a manual mark or the latest local daily bar.
- The `观察持仓` page now also shows both the historical verified ensemble rank and the latest unlabeled inference rank for the current holding.

## 6. Latest Durable Outputs

The current key files are:

- `reports/weekly/myquant_ensemble_test_predictions.csv`
- `reports/weekly/myquant_ensemble_test_metrics.json`
- `reports/weekly/myquant_ensemble_weights.json`
- `reports/weekly/myquant_ensemble_stability.json`
- `reports/weekly/myquant_overlay_latest_packet.json`
- `reports/weekly/myquant_overlay_latest_candidates.csv`
- `reports/weekly/myquant_overlay_latest_brief.md`
- `reports/weekly/myquant_overlay_llm_requests.jsonl`
- `reports/weekly/myquant_overlay_llm_responses.jsonl`
- `reports/weekly/myquant_overlay_llm_response_summary.md`
- `reports/weekly/myquant_inference_packet.json`
- `reports/weekly/myquant_ensemble_inference_predictions.csv`
- `reports/weekly/myquant_overlay_inference_packet.json`
- `reports/weekly/myquant_overlay_inference_candidates.csv`
- `reports/weekly/myquant_overlay_inference_brief.md`
- `reports/weekly/myquant_overlay_inference_llm_requests.jsonl`
- `reports/weekly/myquant_overlay_inference_llm_responses.jsonl`
- `reports/weekly/myquant_overlay_inference_llm_response_summary.md`
- `reports/weekly/000078_watch_plan_2026-04-02.md`
- `reports/weekly/000078_action_memo_2026-04-02.md`

Position-note rule:

- `*_watch_plan_*.md` is now system-generated by `src.agents.watch_plan`.
- `*_action_memo_*.md` is now system-generated by `src.agents.action_memo`.

## 7. Latest Model Snapshot

Based on the refreshed MyQuant reports:

- Ensemble latest signal date: `2026-03-18`
- Ensemble `test` annualized return: about `17.47%`
- Ensemble `test` Sharpe: about `0.88`
- Ensemble `test` max drawdown: about `-13.78%`
- Stability / weight mode: `validation_adaptive`
- Adaptive weights:
  - Ridge: about `77.13%`
  - LightGBM: about `22.87%`

Interpretation:

- Ridge is still the dominant model.
- The refreshed system remains usable as a ranking aid, but the actionable candidate list still lags the raw bar panel because of the 10-day label horizon.
- That lag is now explicitly handled by the separate latest unlabeled inference artifact family instead of by overwriting historical backtest files.

## 8. Latest Candidate Snapshot

From `reports/weekly/myquant_overlay_latest_candidates.csv`:

- Overlay latest date: `2026-03-18`
- Current risk state: trend-on / risk filter active
- Top names in the latest AI overlay include:
  - `300136.SZ 信维通信`
  - `603444.SH 吉比特`
  - `002739.SZ 万达电影`
  - `600967.SH 内蒙一机`
  - `603927.SH 中科软`
  - `002624.SZ 完美世界`

Practical interpretation:

- For `2026-04-02` daytime observation, these names should be checked against their actual `2026-04-01` structure before treating them as buy candidates.
- `002624.SZ 完美世界` was one of the cleaner names after the refresh because it closed back above both 20-day and 60-day averages on `2026-04-01`.

Latest unlabeled inference snapshot:

- Source file: `reports/weekly/myquant_overlay_inference_candidates.csv`
- Inference date: `2026-04-01`
- Risk state: `trend_on`
- Top names in the new unlabeled AI overlay include:
  - `002583.SZ 海能达`
  - `600967.SH 内蒙一机`
  - `603766.SH 隆鑫通用`
  - `300058.SZ 蓝色光标`
  - `002271.SZ 东方雨虹`

Interpretation:

- This is the newest system-generated candidate list tied to the actual `2026-04-01` feature snapshot.
- It is more actionable for next-day observation than the historical `2026-03-18` overlay, but it is still an inference because next-day tradability is not yet observable on the latest unlabeled date.

## 9. Recovery Checklist

If the chat is lost again, recover in this order:

1. Read `docs/README.md`.
2. Read `docs/PROJECT_ARCHITECTURE.md`.
3. Read this file.
4. Check dashboard state:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\streamlit_status.ps1
```

5. If needed, restart the dashboard:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\stop_streamlit.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\start_streamlit.ps1
```

6. Read the current position notes:

```text
reports/weekly/000078_watch_plan_2026-04-02.md
reports/weekly/000078_action_memo_2026-04-02.md
```

7. If the holding notes are missing or stale, regenerate them:

```powershell
python -m src.agents.watch_plan
python -m src.agents.action_memo
```

## 10. Known Issues To Remember

- No Git history is available here.
- The candidate pool is still delayed relative to raw bars because the active target is `ret_t1_t10`.
- Same-day buy judgment still needs human review against the latest bar structure and recent announcements.
- Same-day MyQuant refresh depends on the local `掘金量化终端` / `gmterm-serv` being available.

## 11. Immediate Backlog

Recommended next engineering tasks:

1. Add automated tests for:
   - `streamlit_status.ps1` / dashboard status integration
   - `overlay_report.py`
   - `walkforward.py`
2. Decide whether the watchlist view should also surface the latest unlabeled inference rank alongside the historical verified rank.
3. Keep updating the latest `PROJECT_MEMORY_*.md` after each major pipeline refresh.

## 12. UI Changes On 2026-04-02

- `streamlit_app.py` no longer shows download task status or download logs in the dashboard UI.
- Download and refill jobs are now expected to run in a separate CMD or PowerShell window.
- The old `运行日志` tab is now a `页面服务` tab focused on service status, page logs, and access info.
- The `观察持仓` view now supports keyword search, scope filters, and sorting so the watchlist works more like a盘前工作台.

## 13. Architecture Review On 2026-04-02

- A dedicated architecture review was added in `docs/APP_ARCHITECTURE_PROPOSAL_2026-04-02.md`.
- Core conclusion: the research pipeline folders are acceptable, but the application layer is missing, so `streamlit_app.py` has absorbed file IO, orchestration, snapshot assembly, and presentation concerns.
- Recommended direction: keep Streamlit for now, add a new `src/app` layer for repositories, services, view models, and pages, then progressively thin `streamlit_app.py`.
- Suggested first implementation slice:
  1. Extract config/report repositories.
  2. Extract watchlist service.
  3. Split the watchlist tab into an independent page module.
  4. Extract a shared holding snapshot service for `watch_plan` and `action_memo`.

## 14. Phase 1 Refactor Progress On 2026-04-02

- The first implementation slice has now been started in code under `src/app/`.
- Added repositories:
  - `src/app/repositories/config_repository.py`
  - `src/app/repositories/report_repository.py`
- Added services:
  - `src/app/services/holding_snapshot_service.py`
  - `src/app/services/watchlist_service.py`
- Added page module:
  - `src/app/pages/watchlist_page.py`
- `streamlit_app.py` now reads experiment/watchlist/report data through repository wrappers and delegates watchlist snapshot assembly to `src/app/services/watchlist_service.py`.
- The `观察池与持仓` tab is now routed through `src/app/pages/watchlist_page.py`; the previous inline tab body is temporarily retained only as a commented legacy block for safe rollback/reference during the refactor.
- `src/agents/action_memo.py` no longer imports private helpers from `src/agents/watch_plan.py`; both agent modules now share the new holding snapshot service.
- New regression coverage was added in `tests/test_app_layer.py` for:
  - config repository round-trip/default behavior
  - latest markdown note resolution
  - watchlist service composition of holding, inference, and discussion data
- Verified on `2026-04-02`:
  - `py_compile` passes for `streamlit_app.py`, `watch_plan.py`, `action_memo.py`, and the new `src/app/*` modules
  - app-layer regression suite passes:

```powershell
D:\openlianghua\.venv\Scripts\python.exe -m unittest tests.test_app_layer tests.test_watch_plan tests.test_action_memo tests.test_prediction_snapshot tests.test_premarket_plan tests.test_llm_discussion -q
```

- Note: `tests.test_latest_inference` exceeded the local timeout during this refactor pass and should be treated as a separate legacy-performance check; it was not part of the modified code path.

## 15. Phase 1 Closeout On 2026-04-02

- Before finishing the cleanup pass, a rollback snapshot was created at:
  - `backups/phase1_pre_closeout_20260402_085711`
- `streamlit_app.py` has now been trimmed further:
  - removed the legacy watchlist helper path (`_latest_symbol_bar`, `_build_reduce_plan`, `_filtered_watchlist_view`, and the old inline watchlist rendering block)
  - removed the now-unused watchlist-specific imports and cached discussion loader from the app composition root
  - kept only the new watchlist handoff through `src/app/services/watchlist_service.py` and `src/app/pages/watchlist_page.py`
- This means the watchlist tab is no longer running in a “new path + commented old path” hybrid state; Phase 1 for the watchlist slice is effectively closed.
- Verified on `2026-04-02`:

```powershell
D:\openlianghua\.venv\Scripts\python.exe -m py_compile streamlit_app.py src\agents\watch_plan.py src\agents\action_memo.py src\app\repositories\config_repository.py src\app\repositories\report_repository.py src\app\services\holding_snapshot_service.py src\app\services\watchlist_service.py src\app\pages\watchlist_page.py tests\test_app_layer.py

D:\openlianghua\.venv\Scripts\python.exe -m unittest tests.test_app_layer tests.test_watch_plan tests.test_action_memo tests.test_prediction_snapshot tests.test_premarket_plan tests.test_llm_discussion tests.test_holding_marks tests.test_myquant_panel -q
```

## 16. Realtime Quote Integration On 2026-04-02

- The watchlist page now supports intraday quote refresh using AkShare's Eastmoney minute-bar endpoint through:
  - `src/app/services/realtime_quote_service.py`
  - `src/app/pages/watchlist_page.py`
  - `streamlit_app.py`
- Design choice:
  - use AkShare's Sina intraday tick feed as the primary realtime source because it proved more stable on this machine during implementation
  - keep Eastmoney tick and Eastmoney minute-bar endpoints as fallback providers
  - do not depend on Eastmoney spot/bid-ask APIs for now because they were unstable on this machine during implementation
  - derive latest price, intraday change, high/low, and holding PnL from the latest available intraday ticks or minute bars
- UI behavior:
  - the `观察池与持仓` page includes a `刷新实时行情` action
  - the page shows realtime coverage, fetch timestamp, source, intraday price, intraday pct change, and realtime holding PnL
  - when the latest minute bar reaches the main-board limit-up threshold, the symbol detail view shows a dedicated success notice
- The realtime layer is intentionally best-effort:
  - if a symbol fails to fetch minute bars, the page keeps rendering and shows coverage plus failed symbols
  - if the whole fetch fails, the watchlist falls back to the existing end-of-day holding snapshot without blocking the page
- Added regression coverage:
  - `tests/test_realtime_quote_service.py`

## 17. Focus Pool On 2026-04-02

- `config/watchlist.yaml` now supports two layers:
  - `holdings`
  - `focus_pool`
- The watchlist application layer now merges both into one view through `src/app/services/watchlist_service.py`.
- Each row now carries grouping metadata:
  - `entry_group`
  - `focus_note`
  - `is_watch_only`
- The watchlist page now supports viewing the merged pool and filtering by:
  - all
  - holdings only
  - focus pool only
- Current seeded focus pool was created from the latest inference overlay candidates and includes:
  - `002583.SZ` 海能达
  - `600967.SH` 内蒙一机
  - `603766.SH` 隆鑫通用
  - `300058.SZ` 蓝色光标
  - `002271.SZ` 东方雨虹
  - `300567.SZ` 精测电子
  - `002131.SZ` 利欧股份
  - `002155.SZ` 湖南黄金

## 18. Second Tab Extraction On 2026-04-02

- The `AI研判` tab has now been split out of `streamlit_app.py` into:
  - `src/app/pages/ai_review_page.py`
- The app composition root now delegates tab rendering through:
  - `render_ai_review_page(...)`
- Added a small regression test for the new page-level helper:
  - `tests/test_ai_review_page.py`
- Current state of the refactor:
  - the tab entry and rendering logic are now separated into a page module
  - the original overlay helper block in `streamlit_app.py` has now been removed in the follow-up cleanup pass

## 19. Third Tab Extraction On 2026-04-02

- The `候选股票` tab has now been extracted into:
  - `src/app/pages/candidates_page.py`
- The main app now delegates candidate rendering through:
  - `render_candidates_page(...)`
- Added page-level regression coverage:
  - `tests/test_candidates_page.py`
- Cleanup completed in the same pass:
  - removed the old inline candidate snapshot helper from `streamlit_app.py`
  - removed the now-obsolete legacy overlay helper region from `streamlit_app.py`
- Verified after extraction:
  - `py_compile` passes for `streamlit_app.py`, `src/app/pages/ai_review_page.py`, and `src/app/pages/candidates_page.py`
  - 25 targeted regression tests pass
  - dashboard restarts successfully and still serves `http://localhost:8501`


## 20. Refresh Performance And Final Tab Split On 2026-04-02

- Solved the main refresh latency problem in `streamlit_app.py`:
  - removed the `st.tabs(...)` full-page execution pattern
  - replaced it with a single `st.radio(...)` workspace switcher so each rerun only renders one active page
  - removed eager top-level loading of feature panel, prediction tables, overlay packets, candidate snapshots, and watchlist data
- Realtime quote behavior is now explicit-only for the watchlist page:
  - no automatic intraday fetch on page refresh
  - watchlist realtime data is only requested when the user clicks the `??????` button
  - latest fetched realtime snapshot is kept in session state and merged into the watchlist view when available
- Completed the remaining page extraction work by adding:
  - `src/app/pages/overview_page.py`
  - `src/app/pages/factor_explorer_page.py`
  - `src/app/pages/service_page.py`
- `????` is now fully routed through:
  - `src/app/pages/model_backtest_page.py`
- The Streamlit composition root now delegates all main workspace pages through page modules.
- Added regression coverage for the newly extracted page helpers:
  - `tests/test_overview_page.py`
  - `tests/test_factor_explorer_page.py`
  - `tests/test_service_page.py`
- Verified after the performance refactor:
  - `py_compile` passes for `streamlit_app.py` and all extracted page modules
  - 35 targeted regression tests pass under the project virtualenv
  - dashboard restart and HTTP verification were re-run after the refactor


## 21. Duplicate Button Id Fix On 2026-04-02

- Fixed a Streamlit `StreamlitDuplicateElementId` crash caused by duplicated sidebar button labels after the recent UI refactor.
- Added explicit stable keys to the sidebar controls in `streamlit_app.py`, including:
  - date inputs
  - label/top-n parameter widgets
  - config submit button
  - all sidebar action buttons
- Re-verified after the fix:
  - `py_compile` passes for `streamlit_app.py`
  - the dashboard starts cleanly again
  - `streamlit_status.ps1` reports `running`
  - HTTP check for `http://localhost:8501` returns `200`


## 22. Main UI Chinese Label Repair On 2026-04-02

- Fixed a UI text regression where the main Streamlit shell in `streamlit_app.py` showed `?` placeholders instead of Chinese labels.
- Root cause:
  - the previous whole-block rewrite of the main page shell had written the top-level labels as literal question marks
  - page modules themselves were mostly intact; the main composition block was the damaged area
- Repaired the user-visible Chinese text for:
  - page title and caption
  - workspace switcher labels
  - sidebar service summary labels
  - experiment parameter form labels
  - sidebar action buttons and spinner text
  - current page routing labels
- Re-verified after repair:
  - no remaining `?` placeholder lines in `streamlit_app.py`
  - `py_compile` passes
  - dashboard restart succeeds
  - `streamlit_status.ps1` reports `running`
  - `http://localhost:8501` returns `200`


## 23. Frontend Shell Refactor On 2026-04-02

- Implemented the planned frontend shell cleanup so `streamlit_app.py` now behaves as a composition root instead of a mixed UI script.
- Added a dedicated shell and registry layer:
  - `src/app/ui/app_shell.py`
  - `src/app/ui/sidebar_actions.py`
  - `src/app/ui/ui_text.py`
  - `src/app/page_registry.py`
- The main app now delegates through:
  - page registry construction
  - shell rendering
  - sidebar rendering
  - active page dispatch
- Added focused regression coverage for the new shell layer:
  - `tests/test_page_registry.py`
  - `tests/test_app_shell.py`
  - `tests/test_sidebar_actions.py`
- Verified after the refactor:
  - `py_compile` passes for the new shell modules and `streamlit_app.py`
  - 43 targeted regression tests pass under the project virtualenv
  - dashboard restart and HTTP verification were re-run after the shell refactor


## 24. Parallel React Frontend And API On 2026-04-02

- Started the parallel migration to `React + API` without deleting the existing Streamlit frontend.
- Added the new Python API layer:
  - `src/web_api/app.py`
  - `src/app/facades/dashboard_facade.py`
  - `src/app/services/streamlit_runtime_service.py`
- Added a new React frontend workspace:
  - `web/`
  - Vite + React + TypeScript
  - React Router
  - TanStack Query
  - Recharts
  - react-markdown
- Current React shell includes migrated pages for:
  - overview
  - factor explorer
  - model backtest
  - candidates
  - watchlist
  - AI review
  - service
- Added startup scripts:
  - `scripts/start_research_api.ps1`
  - `scripts/start_react_web.ps1`
- Added migration/runbook documentation:
  - `docs/REACT_PARALLEL_FRONTEND_2026-04-02.md`
- Verified in this pass:
  - API test client returns `200` for `/api/meta`, `/api/overview`, and `/api/service`
  - React production build passes
  - Python API and new frontend support files compile
  - existing Streamlit path remains untouched as fallback


## 25. React UX And Shared Dashboard Data Layer On 2026-04-02

- Continued the parallel frontend work in two directions at once:
  - improved the actual React usability
  - sank shared dashboard loading/caching into a reusable data service
- Added a shared loader/runtime module:
  - `src/app/services/dashboard_data_service.py`
- That shared module now centralizes:
  - model/split/bootstrap constants
  - experiment/watchlist config read and save
  - cached dataset/prediction/report loading
  - watchlist base frame construction
  - sidebar action discovery
  - module execution helper
- Rewired `src/app/facades/dashboard_facade.py` onto the shared data service so the API no longer maintains its own duplicate cache/load layer.
- Rewired `streamlit_app.py` loaders to call the same shared data service, while keeping the existing Streamlit UI intact.
- Improved React usability:
  - added `QueryNotice` for loading and error visibility
  - action buttons now show pending state and disable while running
  - config save and cache clear now show busy state
  - key page filters now persist in the URL on:
    - overview
    - factor explorer
    - model backtest
    - candidates
    - watchlist
    - AI review
  - updated `web/index.html` title to `OpenLianghua React 控制台`
- React/API live run verification:
  - API started successfully and `http://127.0.0.1:8000/api/meta` returned `200`
  - React dev frontend started successfully and `http://127.0.0.1:5173` returned `200`
  - Vite proxy to `http://127.0.0.1:5173/api/service` returned `200`
- Regression verification in this pass:
  - `python -m py_compile` passed for `streamlit_app.py`, `dashboard_data_service.py`, `dashboard_facade.py`, and `src/web_api/app.py`
  - `tests.test_web_api`, `tests.test_app_shell`, `tests.test_sidebar_actions`, `tests.test_page_registry`, and `tests.test_app_layer` passed
  - React `npm run build` passed after the UX updates


## 26. Shared Facade Payloads For Streamlit Workspaces On 2026-04-02

- Continued the convergence work so Streamlit and React now consume the same facade-level page payloads for:
  - overview
  - factor explorer
  - model backtest
  - candidates
  - watchlist
  - AI review
  - service status
- Added payload-oriented Streamlit render entrypoints and a small shared record helper:
  - `src/app/pages/payload_utils.py`
  - payload renderers inside the page modules under `src/app/pages/`
- Rewired `streamlit_app.py` workspace dispatch to request page payloads from `src/app/facades/dashboard_facade.py` instead of assembling per-page data locally.
- Extended watchlist payloads with realtime refresh context:
  - `refreshSymbols`
  - `refreshPreviousCloses`
  - `filters`
- Added `apply_realtime_to_watchlist_payload(...)` to merge deferred realtime quotes back into the shared watchlist payload so Streamlit keeps explicit refresh behavior while still using the same data contract as React/API.
- Hardened realtime merge behavior for partial record sets so facade payload tests and reduced API payloads no longer fail when `shares`, `cost_basis`, or `mark_price` are absent.
- Verified in this pass:
  - `tests.test_dashboard_facade`
  - `tests.test_realtime_quote_service`
  - `tests.test_dashboard_data_service`
  - `tests.test_web_api`
  - `python -m py_compile` for updated facade/page/service modules
  - HTTP verification for Streamlit and API after the payload convergence


## 27. Shared Shell Contract For Sidebar Actions On 2026-04-02

- Continued the convergence one layer higher by sinking the shell/sidebar contract into the facade.
- Added a new shared shell payload in `src/app/facades/dashboard_facade.py`:
  - `get_shell_payload()`
  - includes:
    - `bootstrap`
    - `experimentConfig`
    - `service`
    - `watchlistEntryCount`
    - `configSummaryText`
- Added shared cache/action facade helpers:
  - `clear_cache_payload()`
  - existing `run_named_action()` now serves as the shared action contract for Streamlit and API
- Centralized sidebar action metadata in `src/app/services/dashboard_data_service.py`:
  - action label
  - spinner text
  - module name
  - stable button key
- Reworked `src/app/ui/sidebar_actions.py` so Streamlit sidebar now renders from shell payload data instead of its own private action spec list.
- Reworked `streamlit_app.py` to:
  - load one shell payload for sidebar/service/config shell state
  - save config through `update_experiment_config_payload`
  - execute actions through `run_named_action`
  - clear cache through `clear_cache_payload`
- Added `/api/shell` in `src/web_api/app.py` and switched the React app shell to consume it instead of separately fetching `/api/meta`, `/api/config/experiment`, and `/api/service`.
- Cleaned old Streamlit-only shell loaders/imports that were no longer on the active path.
- Verified in this pass:
  - `python -m py_compile` for Streamlit, facade, data service, sidebar UI, and API
  - `tests.test_dashboard_facade`
  - `tests.test_dashboard_data_service`
  - `tests.test_sidebar_actions`
  - `tests.test_web_api`
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:8501`
    - `http://127.0.0.1:8000/api/shell`
    - `http://127.0.0.1:5173/api/shell`


## 28. React Page-Level Facade Client On 2026-04-02

- Continued the React cleanup by extracting page-level query and URL state contracts into shared front-end facade helpers.
- Added new front-end facade helpers:
  - `web/src/facades/dashboardPageClient.ts`
  - `web/src/facades/pageUrlState.ts`
  - `web/src/facades/usePageSearchState.ts`
- These helpers now centralize:
  - page query keys
  - page API path construction
  - URL search param parsing and defaults
  - URL search param update behavior
  - watchlist realtime path and action endpoints
- Rewired these React pages to use the shared page facade client instead of locally hand-building `queryKey`, API URLs, and search-param mutations:
  - `web/src/pages/OverviewPage.tsx`
  - `web/src/pages/FactorExplorerPage.tsx`
  - `web/src/pages/ModelBacktestPage.tsx`
  - `web/src/pages/CandidatesPage.tsx`
  - `web/src/pages/WatchlistPage.tsx`
  - `web/src/pages/AiReviewPage.tsx`
  - `web/src/pages/ServicePage.tsx`
- Rewired `web/src/App.tsx` to use the same facade path for `/api/shell`.
- Verification in this pass:
  - `npm run build`
  - `tests.test_dashboard_facade`
  - `tests.test_dashboard_data_service`
  - `tests.test_web_api`
- live checks:
  - `http://127.0.0.1:5173`
  - `http://127.0.0.1:5173/api/shell`
- Structural check also confirmed the page files no longer contain direct page endpoint strings or `useSearchParams` calls.


## 29. React Shared Display Blocks On 2026-04-02

- Continued the React cleanup by extracting repeated display-layer JSX from page modules.
- Added new shared display components:
  - `web/src/components/ControlField.tsx`
  - `web/src/components/ControlGrid.tsx`
- Upgraded shared data display components:
  - `web/src/components/DataTable.tsx`
    - now supports `loading` and `loadingText`
  - `web/src/components/LineChartCard.tsx`
    - now auto-derives line keys when omitted
    - now supports custom empty text
- Reworked these page modules onto the shared display blocks:
  - `web/src/pages/OverviewPage.tsx`
  - `web/src/pages/FactorExplorerPage.tsx`
  - `web/src/pages/ModelBacktestPage.tsx`
  - `web/src/pages/CandidatesPage.tsx`
  - `web/src/pages/WatchlistPage.tsx`
  - `web/src/pages/AiReviewPage.tsx`
- This removed repeated page-level JSX for:
  - label + input/select wrappers
  - `control-grid` layout markup
  - loading-vs-empty `DataTable` ternaries
  - manual chart line-key derivation from first-row object keys
- Verification in this pass:
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/api/shell`
  - structural grep confirmed the page modules no longer contain raw `control-grid`, manual line-key derivation, or loading-empty ternary patterns


## 30. React UX Polish On 2026-04-02

- Continued the React frontend polish with a product-facing pass focused on shell consistency, table readability, and the information hierarchy of the watchlist and AI review pages.
- Added shared view-layer building blocks:
  - `web/src/components/Badge.tsx`
  - `web/src/components/PropertyGrid.tsx`
  - `web/src/components/InsightList.tsx`
- Upgraded shared formatting and table behavior:
  - `web/src/lib/format.ts`
    - centralized friendly field labels
    - added field-aware date/percent/cell formatting
    - added helpers for turning records into vertical field sheets
  - `web/src/components/DataTable.tsx`
    - friendly column headers
    - sticky first column support
    - better loading/empty wording
- Reworked the main shell:
  - `web/src/layout/AppLayout.tsx`
  - `web/src/App.tsx`
  - new sticky workspace toolbar with current-page context
  - cleaned all previously garbled React shell copy
- Reworked the two most operator-facing pages:
  - `web/src/pages/WatchlistPage.tsx`
    - selected holding now shows summary cards first
    - raw record moved behind a collapsible full-field sheet
    - watchlist table reduced to decision-relevant columns
    - realtime and discussion state grouped into a dedicated status panel
  - `web/src/pages/AiReviewPage.tsx`
    - selected candidate now shows headline, score strip, tags, bullish/risk lists, and digest cards before the table
    - full candidate record and optional LLM response moved behind collapsible field sheets
    - candidate tables now emphasize the key columns instead of dumping every field
- Reworked global React styling in `web/src/index.css`:
  - unified shell/toolbar visual treatment
  - improved table zebra/hover/fixed-column behavior
  - added badge, property-grid, insight-card, and details-block styles
- Verification in this pass:
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/api/shell`
    - `http://127.0.0.1:5173/api/watchlist`
    - `http://127.0.0.1:5173/api/ai-review`
  - structural grep confirmed the previously garbled mojibake-like strings are no longer present in `web/src`


## 31. Candidate And Backtest UX Polish On 2026-04-03

- Continued the React productization pass on the remaining operator-heavy pages:
  - `web/src/pages/CandidatesPage.tsx`
  - `web/src/pages/ModelBacktestPage.tsx`
- Reworked `CandidatesPage` from a plain table + chart view into a summary-first candidate workspace:
  - current selected stock now gets a headline area with rank and quick status badges
  - key score / rank / forward-return metrics are surfaced before the table
  - candidate details are summarized through `PropertyGrid`
  - the main candidate table is now reduced to the fields most useful for fast comparison
- Reworked `ModelBacktestPage` from a metrics dump into a strategy review workspace:
  - strategy headline now surfaces model, split, stability grade, and conclusion
  - stability checks are separated into passed vs. needs-attention lists
  - strategy snapshot now surfaces observations, turnover, risk-filter ratio, and benchmark context
  - portfolio curve and period-return-vs-benchmark curve are split into separate chart cards
  - monthly / importance / yearly / regime diagnostics now use curated columns and friendlier labels
- Also refreshed:
  - `web/src/components/LineChartCard.tsx`
    - cleaned empty-state copy
- Verification in this pass:
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/api/candidates`
    - `http://127.0.0.1:5173/api/backtests`
    - `http://127.0.0.1:5173/api/shell`


## 32. Overview Factor Service UX Polish On 2026-04-03

- Continued the React first-screen cleanup across the remaining workspace pages:
  - `web/src/pages/OverviewPage.tsx`
  - `web/src/pages/FactorExplorerPage.tsx`
  - `web/src/pages/ServicePage.tsx`
- Reworked `OverviewPage` into a summary-first dashboard:
  - split selector remains at the top
  - first screen now highlights dataset coverage, file health, and best model snapshots
  - model comparison is reduced to curated columns
  - equity curves remain lower as the main time-series view
- Reworked `FactorExplorerPage` into a factor-workbench layout:
  - first screen now focuses on the selected factor, top-ranked stock, worst missing-rate signal, and selected symbol snapshot
  - factor ranking and missing-rate tables now use curated columns instead of raw payload dumps
  - factor history remains as the main chart section
- Reworked `ServicePage` into an operational status page:
  - first screen now summarizes running state, listener state, PID health, restart count, and status freshness
  - raw service fields are kept in a secondary vertical table
  - stdout and stderr remain as dedicated log panels
- Verification in this pass:
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/api/overview`
    - `http://127.0.0.1:5173/api/factors`
    - `http://127.0.0.1:5173/api/service`


## 33. React Visual Unification On 2026-04-03

- Continued the React polish by moving from page-by-page cleanup to shared visual unification.
- Reworked shared UI primitives:
  - `web/src/components/Panel.tsx`
    - now supports `eyebrow` and `tone`
    - standardized header structure for all pages
  - `web/src/components/MetricCard.tsx`
    - now supports helper text
  - `web/src/components/QueryNotice.tsx`
    - cleaned copy and clarified loading/error treatment
  - `web/src/components/ControlField.tsx`
    - now renders dedicated label/control wrappers for consistent form rhythm
  - `web/src/components/DataTable.tsx`
    - now supports `density`
    - keeps column-label and sticky-column features while standardizing table sizing
- Reworked `web/src/index.css` to unify the visual system across pages:
  - tighter panel/header rhythm
  - stronger button hierarchy and hover/active feedback
  - denser, more readable tables with sticky-column separation
  - clearer loading/error notice states
  - more consistent control-field labeling and spacing
  - improved card shadow, border, and first-screen visual cadence
- Verification in this pass:
  - structural grep confirmed no mojibake-like text remains in `web/src`
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/api/shell`
    - `http://127.0.0.1:5173/api/watchlist`
    - `http://127.0.0.1:5173/api/backtests`


## 34. React Interaction Layer On 2026-04-03

- Continued the React operator-workstation pass by adding the first shared interaction layer instead of only polishing layout and copy.
- Added global toast handling:
  - `web/src/components/ToastProvider.tsx`
    - provides unified success / error / info feedback
    - now used by shell-level config save, action execution, and cache clear flows
- Added a shared right-side detail drawer:
  - `web/src/components/DetailDrawer.tsx`
    - provides a common detail surface with title, subtitle, meta, body, and footer slots
    - supports backdrop close and escape-to-close
- Reworked `web/src/components/DataTable.tsx` into an interactive operational table:
  - supports header sorting
  - supports column visibility management
  - supports row click, row highlight, and selected-row state
  - keeps sticky-first-column behavior for wide tables
- Reworked `web/src/pages/WatchlistPage.tsx`:
  - watchlist table now supports sorting and field show/hide
  - row click now opens the holding detail drawer
  - selected holding summary now also exposes an explicit "open drawer" action
  - local realtime refresh feedback is now shown inline with clearer status wording
  - watch-plan generation, action-memo generation, and realtime refresh now emit unified toasts
- Reworked `web/src/pages/CandidatesPage.tsx`:
  - candidate table now supports sorting and field show/hide
  - row click now opens the candidate detail drawer
  - selected candidate summary now also exposes an explicit "open drawer" action
- Extended `web/src/index.css` with the shared interaction-layer styling:
  - table toolbar and column manager
  - interactive row highlight state
  - drawer surface and backdrop
  - toast viewport and tone styles
  - future-ready theme tokens through `:root[data-theme='light']` and `:root[data-theme='dark']`
- Verification in this pass:
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/api/shell`
    - `http://127.0.0.1:5173/api/watchlist`
    - `http://127.0.0.1:5173/api/candidates`


## 35. React Theme And Persistence Pass On 2026-04-03

- Continued the React interaction work by moving from one-off interactions to shared UI preferences and operator shortcuts.
- Added theme infrastructure:
  - `web/src/components/ThemeProvider.tsx`
    - manages light / dark theme state
    - persists the selected theme in browser storage
    - writes `data-theme` to the document root so future token work can stay CSS-driven
  - `web/src/layout/AppLayout.tsx`
    - now exposes a visible theme switch in the workspace toolbar
- Added shared preference persistence helpers:
  - `web/src/lib/uiPreferences.ts`
    - stores theme preference
    - stores per-table visible-column selections
- Extended `web/src/components/DataTable.tsx`:
  - now supports a `storageKey`
  - visible columns are restored from local storage when the table mounts
  - visible columns are persisted after each field-show/hide change
  - main operational tables now also expose a "restore default" action
- Reworked `web/src/pages/WatchlistPage.tsx`:
  - main watchlist matrix now persists visible columns
  - reduce-plan table now also persists visible columns
  - detail drawer now exposes quick actions:
    - copy symbol
    - open AI review for the selected symbol
    - refresh realtime quotes
    - generate watch plan
    - generate action memo
- Reworked `web/src/pages/CandidatesPage.tsx`:
  - main candidate matrix now persists visible columns
  - detail drawer now exposes quick actions:
    - copy symbol
    - open AI review for the selected symbol
- Refreshed shared polish:
  - `web/src/App.tsx`
    - shell-level save / action / clear-cache flow now stays on the shared theme + toast stack
  - `web/src/components/ToastProvider.tsx`
    - cleaned close control and toast copy handling
  - `web/src/components/DetailDrawer.tsx`
    - cleaned close control copy
  - `web/src/index.css`
    - added theme-aware surface tokens
    - added active-theme button treatment
    - added theme switch styling
    - replaced several light-only surfaces with tokenized surfaces so dark mode is usable now rather than only scaffolded
- Verification in this pass:
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/api/shell`
    - `http://127.0.0.1:5173/api/watchlist`
    - `http://127.0.0.1:5173/api/candidates`


## 36. Preference Reset And Wider Table Persistence On 2026-04-03

- Continued the React preference work by extending visible-column persistence beyond holdings/candidates and by adding a global reset entry point.
- Reworked `web/src/lib/uiPreferences.ts`:
  - storage helpers now also support clearing UI preferences
  - clearing preferences now removes:
    - stored theme preference
    - all stored table visible-column preferences
  - clearing preferences now also emits a browser event so already-mounted tables reset immediately instead of only after a page refresh
- Reworked `web/src/components/DataTable.tsx`:
  - tables with `storageKey` now listen for the shared UI-preference reset event
  - on reset, visible columns return to defaults immediately
  - on reset, local table sort state is also cleared
- Reworked `web/src/App.tsx`:
  - shell layer now owns a `reset UI preferences` mutation
  - reset flow now clears browser preference storage, restores the theme, and emits a unified success/error toast
- Reworked `web/src/layout/AppLayout.tsx`:
  - the workspace toolbar now exposes a visible `重置界面偏好` action next to the light/dark theme switch
- Extended field persistence to more React research pages:
  - `web/src/pages/AiReviewPage.tsx`
    - both inference and historical candidate tables now persist visible columns independently
  - `web/src/pages/FactorExplorerPage.tsx`
    - factor snapshot, factor ranking, and missing-rate tables now persist visible columns
  - `web/src/pages/ModelBacktestPage.tsx`
    - monthly summary, feature importance, yearly diagnostics, and regime diagnostics tables now persist visible columns
  - `web/src/pages/ServicePage.tsx`
    - raw service-field table now persists visible columns
- Verification in this pass:
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/ai-review`
    - `http://127.0.0.1:5173/factors`
    - `http://127.0.0.1:5173/backtests`
    - `http://127.0.0.1:5173/service`
    - `http://127.0.0.1:5173/api/shell`
    - `http://127.0.0.1:5173/api/ai-review`
    - `http://127.0.0.1:5173/api/factors`
    - `http://127.0.0.1:5173/api/backtests`
    - `http://127.0.0.1:5173/api/service`


## 37. Drag Reorder And Page View Presets On 2026-04-03

- Continued the React table-preference work by moving from static field show/hide into a fuller table workspace model.
- Reworked `web/src/components/DataTable.tsx`:
  - columns can now be drag-reordered directly from table headers
  - reordered columns persist through the shared table preference storage
  - tables can now expose page-specific `viewPresets`
  - preset selection is stored together with visible-column order
  - switching presets now resets the visible column set to the preset-defined order
- Reworked `web/src/lib/uiPreferences.ts`:
  - table preference storage now persists both:
    - visible columns / column order
    - selected preset key
  - legacy array-only stored preferences remain backward-compatible through normalization
- Reworked `web/src/index.css`:
  - added table preset toolbar styling
  - added drag affordance styling
  - added drop-target styling for header reordering feedback
- Reworked page-level defaults and presets:
  - `web/src/pages/WatchlistPage.tsx`
    - main watchlist matrix now has `交易视图` / `排名视图`
    - default preset is `交易视图`
  - `web/src/pages/CandidatesPage.tsx`
    - main candidate matrix now has `决策视图` / `动量视图`
    - default preset is `决策视图`
  - `web/src/pages/AiReviewPage.tsx`
    - inference and historical candidate tables now have `论点视图` / `分数视图`
    - default preset is `论点视图`
  - `web/src/pages/FactorExplorerPage.tsx`
    - ranking table now has `精简视图` / `关注视图`
    - missing-rate table now has `风险视图`
  - `web/src/pages/ModelBacktestPage.tsx`
    - importance table now has `Gain 视图` / `完整视图`
    - yearly/regime diagnostic tables now have `绩效视图` / `执行视图`
- Verification in this pass:
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/watchlist`
    - `http://127.0.0.1:5173/candidates`
    - `http://127.0.0.1:5173/ai-review`
    - `http://127.0.0.1:5173/factors`
    - `http://127.0.0.1:5173/backtests`
    - `http://127.0.0.1:5173/service`
    - `http://127.0.0.1:5173/api/watchlist`
    - `http://127.0.0.1:5173/api/candidates`
    - `http://127.0.0.1:5173/api/ai-review`
    - `http://127.0.0.1:5173/api/factors`
    - `http://127.0.0.1:5173/api/backtests`
    - `http://127.0.0.1:5173/api/service`

## 38. Column Width Memory And Shareable View Links On 2026-04-03

- Expanded the React table preference model so it now persists:
  - visible columns / column order
  - selected preset key
  - per-column widths
- Reworked `web/src/components/DataTable.tsx` so the shared table contract now supports:
  - drag-based column reordering
  - field show/hide
  - preset switching
  - column resize handles
  - double-click reset for a single column width
  - restoring a shared table layout from a URL `view` param
- Reworked `web/src/lib/uiPreferences.ts`:
  - table preferences now store `columns + presetKey + widths`
  - added raw snapshot reads to support share-link generation without page-specific duplication
- Reworked `web/src/facades/pageUrlState.ts`:
  - added route-aware table preference collection for each major page
  - added encoded page-view sharing payloads
  - share URLs now include current page query params plus per-page table layout state
- Reworked React shell:
  - `web/src/App.tsx`
    - added `复制分享链接`
    - copying a link now includes the current page route, current page query state, and current shared table layout state
  - `web/src/layout/AppLayout.tsx`
    - top toolbar now exposes the share action beside theme/reset controls
- Page integration updates:
  - `web/src/pages/OverviewPage.tsx`
    - overview comparison table now has `storageKey="overview-comparison"` so its layout can persist and be shared
- Verification in this pass:
  - targeted eslint on:
    - `src/App.tsx`
    - `src/layout/AppLayout.tsx`
    - `src/components/DataTable.tsx`
    - `src/lib/uiPreferences.ts`
    - `src/facades/pageUrlState.ts`
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/watchlist`
    - `http://127.0.0.1:5173/ai-review`
    - `http://127.0.0.1:5173/api/shell`

## 39. Drawer Share Actions And Expanded Shareable Tables On 2026-04-03

- Added a shared drawer action contract for React detail drawers:
  - `web/src/components/DrawerEntityActions.tsx`
  - one place for:
    - copy symbol
    - copy current view link
    - jump to AI review
    - jump to watchlist / candidates when relevant
    - page-specific extra actions
- Added shared share-link utilities:
  - `web/src/lib/shareLinks.ts`
  - `copyShareablePageLink`
  - route builders for AI review / watchlist / candidates
- Reworked page integrations:
  - `web/src/pages/WatchlistPage.tsx`
  - `web/src/pages/CandidatesPage.tsx`
  - `web/src/pages/AiReviewPage.tsx`
- Expanded page view sharing coverage in:
  - `web/src/facades/pageUrlState.ts`
  - watchlist detail / discussion tables now participate in shared view state
  - candidates detail table now participates in shared view state
  - AI review inference / historical tables now participate in shared view state

## 40. AKShare Fallback Refresh And Watch Symbol Repair On 2026-04-03

- MyQuant refresh was blocked on this machine because the terminal SDK was unavailable.
- Switched the active data source in `config/universe.yaml` to `akshare` and refreshed the market panel to `2026-04-02`.
- Fixed an AKShare compatibility bug in `src/agents/overlay_report.py`:
  - overlay industry mapping now works when `daily_bar.parquet` has `industry` but no `industry_current`
  - added regression test `tests/test_overlay_report.py`
- Fixed an AKShare robustness issue in `src/data/akshare_client.py`:
  - `current_st_symbols()` now degrades to an empty set if the public ST endpoint disconnects
  - added regression test `tests/test_akshare_client.py`
- Fixed an AKShare coverage issue in `src/data/downloader.py`:
  - downloader now unions `watch_symbols` into the download set even in `current_index` mode
  - added regression test `tests/test_downloader.py`
- After the repair:
  - `data/staging/daily_bar.parquet` max date is `2026-04-02`
  - `000078.SZ` is now present again in the local daily panel through `2026-04-02`

## 41. Research Rerun Snapshot For 2026-04-02 Data

- Rebuilt the feature panel after watch-symbol repair:
  - feature panel rows increased to `542,293`
- Reran:
  - `src.models.train_linear`
  - `src.models.train_lgbm`
  - `src.models.train_ensemble`
  - `src.agents.overlay_report`
  - `src.agents.overlay_inference_report`
  - `src.agents.watch_plan`
  - `src.agents.action_memo`
- Latest key artifacts now include:
  - `reports/weekly/akshare_overlay_latest_candidates.csv`
  - `reports/weekly/akshare_overlay_inference_candidates.csv`
  - `reports/weekly/000078_watch_plan_2026-04-03.md`
  - `reports/weekly/000078_action_memo_2026-04-03.md`
- Removed two temporary malformed outputs that were created before the trade-date repair:
  - `000078_watch_plan_-.md`
  - `000078_action_memo_-.md`
- Ensemble test snapshot after rerun:
  - `rank_ic_mean`: `0.0381`
  - `top_n_forward_mean`: `0.0181`
  - `top_n_hit_rate`: `0.5741`
  - `daily_portfolio_annualized_return`: `0.0837`
  - `daily_portfolio_sharpe`: `0.4670`
  - `daily_portfolio_max_drawdown`: `-0.1893`
- Latest inference overlay leaders on `2026-04-02`:
  - `300058.SZ 蓝色光标`
  - `002195.SZ 岩山科技`
  - `600879.SH 航天电子`
  - `300136.SZ 信维通信`
  - `688375.SH 国博电子`
- Holding snapshot after rerun for `000078.SZ`:
  - local latest close: `3.61`
  - latest bar date: `2026-04-02`
  - cost basis: `3.851`
  - reference PnL: about `-6.26%`
  - system execution line: watch `3.61`, defensive `3.53`, observe `3.72`, pressure `3.80-3.85`

## 42. Verification In This Pass

- Python:
  - `python -m unittest tests.test_overlay_report tests.test_downloader tests.test_akshare_client tests.test_watch_plan tests.test_action_memo tests.test_dashboard_facade tests.test_dashboard_data_service tests.test_web_api -q`
  - result: `18 tests OK`
- Live checks:
  - `http://127.0.0.1:5173` -> `200`
  - `http://127.0.0.1:8000/api/meta` -> `200`
  - `http://127.0.0.1:8501` -> `200`

## 43. React Shell Layout Refresh On 2026-04-03

- The React frontend shell was reworked around the user’s stated UI principles:
  - sidebar + main content workspace
  - responsive-first / fluid layout
  - componentized shell sections
  - more semantic HTML structure
- New layout components:
  - `web/src/components/SidebarSection.tsx`
  - `web/src/components/WorkspaceHero.tsx`
- Reworked shell implementation:
  - `web/src/layout/AppLayout.tsx`
  - now uses:
    - semantic `aside / nav / main / header / section`
    - mobile-first sidebar toggle
    - sticky desktop sidebar
    - sticky workspace hero
    - sidebar-hosted research config + actions
    - condensed page summary in the main hero
- Reworked interaction components for better semantic / visual integration:
  - `web/src/components/DataTable.tsx`
    - table container now exposes a region with live/busy semantics
  - `web/src/components/DetailDrawer.tsx`
    - drawer now uses explicit `aria-labelledby / aria-describedby`
  - `web/src/components/LineChartCard.tsx`
    - chart card now uses figure / figcaption semantics
- Reworked CSS foundation in `web/src/index.css`:
  - fluid shell spacing
  - sidebar navigation cards
  - workspace hero summary block
  - mobile off-canvas sidebar
  - stronger chart / drawer / table integration
  - tighter responsive breakpoints for 1180 / 1080 / 840
- Verification in this pass:
  - `npm run build`
  - targeted eslint on touched React shell/components
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/watchlist`
    - `http://127.0.0.1:5173/ai-review`

## 44. React Page Density Refresh On 2026-04-03

- Added shared page-density primitives:
  - `web/src/components/PageFilterBar.tsx`
  - `web/src/components/SegmentedControl.tsx`
  - `web/src/components/DrawerQuickActions.tsx`
- Updated shared components:
  - `web/src/components/Panel.tsx`
    - now accepts `className` for page-level hierarchy styling
  - `web/src/components/LineChartCard.tsx`
    - now supports `subtitle` and `actions`
  - `web/src/components/QueryNotice.tsx`
  - `web/src/components/MarkdownCard.tsx`
  - `web/src/components/DetailDrawer.tsx`
- Reworked React pages around the new density contract:
  - `web/src/pages/OverviewPage.tsx`
  - `web/src/pages/FactorExplorerPage.tsx`
  - `web/src/pages/ModelBacktestPage.tsx`
  - `web/src/pages/CandidatesPage.tsx`
  - `web/src/pages/WatchlistPage.tsx`
  - `web/src/pages/AiReviewPage.tsx`
- Resulting UX changes:
  - unified in-page filter bar across key research pages
  - chart zones now support view switching where payloads provide multiple usable series
  - watchlist / candidate drawers now expose a top-of-drawer quick-action shelf instead of hiding core actions in the footer
  - summary panels vs. table panels now use clearer primary / secondary visual hierarchy
  - several residual React-side garbled strings in shared components were normalized during this pass
- CSS foundation extended in `web/src/index.css` for:
  - `panel--summary-surface`
  - `panel--table-surface`
  - filter bar
  - segmented controls
  - drawer action shelf
  - tighter section-heading hierarchy
- Verification in this pass:
  - `npm run build`
  - targeted eslint on touched React components/pages
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/watchlist`
    - `http://127.0.0.1:5173/api/shell`

## 45. React Shell Simplification On 2026-04-03

- The React shell was restructured again to match the new operator requirement:
  - top bar fixed across the full viewport
  - lower height / lower copy density
  - only system name + current page name in the top bar
  - left sidebar reduced to navigation only
- Reworked shell:
  - `web/src/layout/AppLayout.tsx`
  - now contains:
    - fixed `app-topbar`
    - slim nav-only sidebar
    - mobile menu toggle in the top bar
    - no config/status/action blocks inside the sidebar
- Added a new functional page to absorb former sidebar tools:
  - `web/src/pages/WorkbenchPage.tsx`
  - now hosts:
    - research parameter form
    - research actions
    - latest action output
    - theme/share/reset interface tools
- Routing updated in:
  - `web/src/App.tsx`
  - new route: `/workspace`
- Service page was refreshed so service visibility remains on-page after the sidebar reduction:
  - `web/src/pages/ServicePage.tsx`
- Shared notification text cleanup also touched:
  - `web/src/components/ToastProvider.tsx`
- CSS shell overrides added in `web/src/index.css` for:
  - `app-topbar`
  - nav-only sidebar layout
  - fixed top offset handling
  - mobile sidebar reveal under the top bar
- Validation in this pass:
  - targeted eslint on `AppLayout/App/WorkbenchPage/ServicePage/ToastProvider`
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/workspace`
    - `http://127.0.0.1:5173/service`
    - `http://127.0.0.1:5173/api/shell`

## 46. Workbench And Account Shell Refinement On 2026-04-03

- The React operator shell was refined again around the latest UX direction:
  - workbench page split into three functional zones:
    - research parameters
    - research actions
    - interface settings
  - sidebar navigation regrouped into clearer menu sections
  - stronger active-page highlighting added for a more product-like left nav
  - theme switch moved to the top-right header
  - top-right account area added:
    - shows `login` when there is no local session
    - shows local user profile + `logout` when a profile exists
- Current implementation remains UI-local only for account state:
  - no backend auth/session exists yet in the Python API
  - local profile is persisted in browser storage through `web/src/lib/userSession.ts`
- Files added:
  - `web/src/components/LoginDialog.tsx`
  - `web/src/lib/userSession.ts`
- Files updated:
  - `web/src/layout/AppLayout.tsx`
  - `web/src/pages/WorkbenchPage.tsx`
  - `web/src/index.css`
- Interaction details after this pass:
  - opening `login` creates a fresh dialog instance each time, so stale form state is not reused
  - account menu closes on outside click
  - theme switching is now part of the permanent shell, not buried inside the workbench
  - workbench page no longer mixes shell controls with research controls in the same block
- Validation in this pass:
  - targeted eslint on:
    - `web/src/layout/AppLayout.tsx`
    - `web/src/pages/WorkbenchPage.tsx`
    - `web/src/components/LoginDialog.tsx`
    - `web/src/lib/userSession.ts`
    - `web/src/App.tsx`
    - `web/src/pages/ServicePage.tsx`
    - `web/src/components/ToastProvider.tsx`
  - React `npm run build`
  - live checks:
    - `http://127.0.0.1:5173`
    - `http://127.0.0.1:5173/workspace`
    - `http://127.0.0.1:5173/service`
    - `http://127.0.0.1:5173/api/shell`

## 47. Postgres Auth And Real Session State On 2026-04-03

- The React shell account area is no longer frontend-local only.
- A real cookie-based auth/session layer now exists in the FastAPI backend.
- Database target used for auth:
  - host: `localhost`
  - port: `5432`
  - db: `quant_db`
  - user: `postgres`
- Backend auth implementation:
  - `src/web_api/auth.py`
  - `src/web_api/settings.py`
  - `src/web_api/app.py`
- New API endpoints:
  - `GET /api/auth/session`
  - `POST /api/auth/login`
  - `POST /api/auth/logout`
- Protected write endpoints now require a valid session cookie:
  - `PUT /api/config/experiment`
  - `POST /api/actions/{action_name}`
  - `POST /api/actions/watch-plan`
  - `POST /api/actions/action-memo`
  - `POST /api/cache/clear`
- Session storage details:
  - auth data is stored in Postgres tables:
    - `app_users`
    - `app_sessions`
  - bootstrap admin user is seeded automatically from `.env`
  - password storage uses salted PBKDF2-HMAC-SHA256
  - browser session uses an `HttpOnly` cookie with `SameSite=Lax`
- Frontend auth integration:
  - `web/src/App.tsx`
  - `web/src/layout/AppLayout.tsx`
  - `web/src/components/LoginDialog.tsx`
  - `web/src/api/client.ts`
  - `web/src/types/api.ts`
  - `web/src/pages/WorkbenchPage.tsx`
  - `web/src/pages/WatchlistPage.tsx`
- Frontend UX after this pass:
  - top-right account area reads real backend session state
  - login dialog now submits `username/password`
  - workbench write actions are disabled when logged out
  - watchlist markdown-generation actions are disabled when logged out
- Config updates:
  - `.env` now explicitly includes `APP_DB_*` and `APP_BOOTSTRAP_*`
  - `.env.example` mirrors those settings
  - `requirements.txt` now includes `psycopg[binary]`
- Validation in this pass:
  - `python -m unittest tests.test_web_api tests.test_web_api_auth -q`
  - `python -m py_compile src/web_api/app.py src/web_api/auth.py src/web_api/settings.py`
  - `npm.cmd run build`
  - `npx.cmd eslint src/App.tsx src/api/client.ts src/layout/AppLayout.tsx src/components/LoginDialog.tsx src/pages/WorkbenchPage.tsx src/pages/WatchlistPage.tsx`
  - real Postgres smoke test:
    - login -> session -> logout -> logged-out session all verified through `TestClient`
  - auth tables confirmed in `quant_db`:
    - `app_users`
    - `app_sessions`
- Important current state:
  - code, tests, and database auth path are complete
  - the existing process already listening on port `8000` is still the old API worker and could not be stopped in this session because of local process permissions
  - because of that, live `http://127.0.0.1:8000/api/auth/session` is still returning `404` until the local API process is restarted
- Data-layer conclusion:
  - research/auth are now mixed:
    - auth/session uses Postgres
    - research data loading still uses file repositories (`config_repository.py`, `report_repository.py`)
  - the dashboard is not yet fully database-backed for feature panels, predictions, portfolios, overlay packets, or watch-plan markdowns
